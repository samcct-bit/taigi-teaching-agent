# 臺語教材 AI Agent Python 相依套件
pymupdf>=1.24.0
pillow>=10.3.0
requests>=2.31.0
jinja2>=3.1.4          # material_generator 互動網頁樣板引擎（templates/interactive_website.html.j2）
python-docx>=1.1.2
python-pptx>=1.0.2
pytest>=8.2.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0   # [standard] 提供 reload=True 所需的 watchfiles
python-multipart>=0.0.9
httpx>=0.27.0

# 本地語音辨識引擎 (STT whisper 模式核心；缺少時會默默降級為 dummy)
faster-whisper>=1.0.0

# 教學影片 Hyperframe 管線旁白（Edge-TTS，免費、台灣腔 zh-TW-YunJheNeural）
# 詳見 docs/教學影片-hyperframe.md
edge-tts>=7.2.0

# 選用：本地 TTS（tts.provider="mms"，facebook/mms-tts-nan）。安裝量大(~2GB)，
# 建議裝在非雲端同步路徑的 venv；未安裝時 mms provider 會自動降級為接音合成。
# pip install torch transformers
# torch>=2.2
# transformers>=4.40

# 雲端部署 (Render / GitHub API) 擴充套件
PyGithub>=2.3.0
python-dotenv>=1.0.1
